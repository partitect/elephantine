# Contributing to Elephantine

Thank you for contributing to Elephantine!

## Development Setup

1. Fork & clone repo
2. Setup virtual environment: uv venv .venv
3. Install dependencies: uv pip install -e ".[dev]"
4. Run tests: pytest -v tests/
